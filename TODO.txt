* Make it modular?
* Add front token to parser type?
* Add Parser a b -> a -> [(a,b)] and (a -> [(a, b)]) -> Parser a b functions.
* Make it so that incremental parsing is free if the type doesn't
  support incrementality.
* Look at code generation with Text.
* Investigate why NoMessagesRFC2616 is slower than MunchRFC2616.
